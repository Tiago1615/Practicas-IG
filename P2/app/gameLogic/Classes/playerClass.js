export class Player{
  constructor(x, y, width, height, color, movement, points){
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.color = color;
    this.movement = movement;
    this.points = points;
  }
}
